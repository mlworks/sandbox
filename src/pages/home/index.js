import React from 'react'

// Components
import Input from 'components/input'

const Home = () => (
  <div>
    home
    <Input maxlength={20} />
    <Input value="testing" maxlength={10} />
    <Input />
  </div>
)

export default Home
